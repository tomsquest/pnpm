"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const encodeRegistry = require("encode-registry");
function absolutePathToRef(absolutePath, pkgName, resolution, standardRegistry) {
    if (resolution.type)
        return absolutePath;
    const registryName = encodeRegistry(standardRegistry);
    if (absolutePath.startsWith(`${registryName}/`)) {
        const ref = absolutePath.replace(`${registryName}/${pkgName}/`, '');
        if (ref.indexOf('/') === -1)
            return ref;
        return absolutePath.replace(`${registryName}/`, '/');
    }
    return absolutePath;
}
exports.absolutePathToRef = absolutePathToRef;
//# sourceMappingURL=shrinkwrap.js.map