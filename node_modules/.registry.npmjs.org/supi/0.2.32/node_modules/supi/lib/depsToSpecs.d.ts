import { Dependencies } from './types';
import { PackageSpec } from 'package-store';
export default function (deps: Dependencies, opts: {
    where: string;
    optionalDependencies: Dependencies;
    devDependencies: Dependencies;
}): PackageSpec[];
export declare function similarDepsToSpecs(deps: Dependencies, opts: {
    where: string;
    dev: boolean;
    optional: boolean;
    optionalDependencies: Dependencies;
    devDependencies: Dependencies;
    existingSpecs: Dependencies;
}): PackageSpec[];
