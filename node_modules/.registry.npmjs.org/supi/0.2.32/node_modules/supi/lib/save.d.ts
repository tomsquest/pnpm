import { DependenciesType } from './getSaveType';
import { Package } from './types';
export default function save(pkgJsonPath: string, packageSpecs: ({
    name: string;
    saveSpec: string;
})[], saveType?: DependenciesType): Promise<Package>;
