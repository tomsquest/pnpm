import { DependenciesType } from './getSaveType';
import { Package } from './types';
export default function (pkgJsonPath: string, removedPackages: string[], saveType?: DependenciesType): Promise<Package>;
