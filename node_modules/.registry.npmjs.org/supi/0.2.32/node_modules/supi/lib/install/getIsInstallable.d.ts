import { Package } from '../types';
import { FetchedPackage } from 'package-store';
export default function getIsInstallable(pkgId: string, pkg: Package, fetchedPkg: FetchedPackage, options: {
    optional: boolean;
    engineStrict: boolean;
    nodeVersion: string;
    pnpmVersion: string;
}): Promise<boolean>;
