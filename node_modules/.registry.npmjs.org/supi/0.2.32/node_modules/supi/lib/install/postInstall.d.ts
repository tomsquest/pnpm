import { Package } from '../types';
export default function postInstall(root: string, opts: {
    rawNpmConfig: Object;
    initialWD: string;
    userAgent: string;
    pkgId: string;
}): Promise<void>;
export declare function npmRunScript(stage: string, pkg: Package, opts: {
    rawNpmConfig: Object;
    pkgId: string;
    modulesDir: string;
    root: string;
    stdio?: string;
}): Promise<any>;
