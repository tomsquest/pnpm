import * as unpackStream from 'unpack-stream';
import { Resolution } from './resolve';
import { Got } from './network/got';
export declare type FetchOptions = {
    pkgId: string;
    got: Got;
    storePath: string;
    offline: boolean;
    prefix: string;
};
export declare type PackageDist = {
    tarball: string;
    registry?: string;
    integrity?: string;
};
export default function fetchResolution(resolution: Resolution, target: string, opts: FetchOptions): Promise<unpackStream.Index>;
export declare function fetchFromTarball(dir: string, dist: PackageDist, opts: FetchOptions): Promise<{}>;
export declare function fetchFromRemoteTarball(dir: string, dist: PackageDist, opts: FetchOptions): Promise<{}>;
