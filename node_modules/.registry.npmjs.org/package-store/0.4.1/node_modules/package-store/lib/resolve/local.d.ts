import { PackageSpec, ResolveOptions, ResolveResult } from '.';
/**
 * Resolves a package hosted on the local filesystem
 */
export default function resolveLocal(spec: PackageSpec, opts: ResolveOptions): Promise<ResolveResult>;
